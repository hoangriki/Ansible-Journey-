Contains YAML-based automation workflows.

Defines tasks executed against managed hosts

Uses Ansible modules for idempotent operations

Supports variables, conditionals, loops, and privilege escalation

Represents declarative infrastructure automation

Purpose: Orchestrates configuration and state management.
