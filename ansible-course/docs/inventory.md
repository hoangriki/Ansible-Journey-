Defines the infrastructure Ansible manages.

Contains host definitions and groupings

Supports static (hosts.ini) or dynamic inventory

Can include group_vars/ and host_vars/ for variable management

Establishes the target nodes for automation

Purpose: Separates infrastructure definition from automation logic.
